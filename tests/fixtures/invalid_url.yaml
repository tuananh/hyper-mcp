plugins:
  valid_plugin:
    url: "file:///path/to/plugin"
    runtime_config:
      skip_tools:
        - "tool1"
        - "tool2"

  invalid_url_plugin:
    url: "not a valid url"
    runtime_config:
      allowed_hosts:
        - "example.com"

  another_valid_plugin:
    url: "https://example.com/plugin"
